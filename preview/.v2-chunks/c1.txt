x;
    gap: 8px;
    justify-content: flex-end;
  }
  .btn-sm {
    border: 0;
    border-radius: 8px;
    padding: 9px 16px;
    font-weight: 800;
    font-size: 12px;
  }
  .btn-sm-primary { background: var(--red); color: white; }
  .btn-sm-primary:hover { background: var(--red-dark); }
  .btn-sm-ghost {
    background: white;
    border: 1.5px solid var(--gray-200);
    color: var(--black);
  }

  /* Metric cards */
  .metrics {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 14px;
  }
  @media (max-width: 760px) {
    .metrics { grid-template-columns: repeat(2, 1fr); }
  }
  .metric {
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-lg);
    padding: 16px 18px;
    box-shadow: var(--shadow-sm);
    display: flex;
    align-items: flex-start;
    gap: 14px;
  }
  .metric-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .metric-icon.red { background: var(--red-icon-bg); color: var(--red); }
  .metric-icon.gray { background: var(--gray-100); color: var(--gray-400); }
  .metric-icon.green { background: var(--ok-bg); color: var(--ok); }
  .metric .label {
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 0.07em;
    color: var(--gray-400);
  }
  .metric .value {
    font-size: 28px;
    font-weight: 800;
    margin-top: 2px;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }
  .metric.good .value { color: var(--ok); }

  /* Needs Attention banner */
  .needs {
    background: var(--warn-bg);
    border: 1.5px solid var(--warn-border);
    border-radius: var(--radius-lg);
    overflow: hidden;
  }
  .needs-hd {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 14px 18px;
    width: 100%;
    border: 0;
    background: transparent;
    text-align: left;
    cursor: pointer;
  }
  .needs-hd:hover { background: rgba(255, 255, 255, 0.35); }
  .needs-icon {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #FDE9C0;
    color: var(--warn-icon);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .needs-title {
    font-size: 14px;
    font-weight: 800;
    color: #7A5A00;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .needs-pill {
    background: #F5D06A;
    color: #7A5A00;
    font-size: 11px;
    font-weight: 800;
    padding: 2px 8px;
    border-radius: 999px;
  }
  .needs-sub {
    font-size: 12px;
    color: #9A7A30;
    margin-top: 2px;
    font-weight: 500;
  }
  .needs-chevron {
    margin-left: auto;
    color: #9A7A30;
    transition: transform 0.2s;
  }
  .needs.open .needs-chevron { transform: rotate(90deg); }
  .needs-body {
    display: none;
    padding: 0 18px 14px;
    flex-direction: column;
    gap: 8px;
  }
  .needs.open .needs-body { display: flex; }
  .needs-item {
    display: flex;
    align-items: flex-start;
    gap: 10px;
    background: rgba(255,255,255,0.75);
    border-radius: 8px;
    padding: 10px 12px;
    font-size: 13px;
    color: var(--gray-600);
  }
  .needs-item .tag {
    font-size: 10px;
    font-weight: 800;
    padding: 2px 7px;
    border-radius: 4px;
    flex-shrink: 0;
    margin-top: 1px;
  }
  .tag.warn { background: #FFE8A3; color: #8a6d00; }
  .tag.info { background: #E8EEFC; color: #001F5B; }
  .tag.alert { background: #FFE0E0; color: var(--red); }
  .needs-item .dismiss {
    margin-left: auto;
    border: 0;
    background: transparent;
    color: var(--gray-400);
    font-size: 16px;
    line-height: 1;
    padding: 0 2px;
    flex-shrink: 0;
  }
  .needs-item .dismiss:hover { color: var(--red); }
  .needs.hidden { display: none; }

  /* Unused drivers */
  .unused-sec {
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
  }
  .unused-hd {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 12px 16px;
    width: 100%;
    border: 0;
    background: transparent;
    text-align: left;
    cursor: pointer;
    font-size: 13px;
    font-weight: 700;
    color: var(--gray-600);
  }
  .unused-hd:hover { background: var(--gray-50); }
  .unused-hd svg { color: var(--gray-400); }
  .unused-count {
    margin-left: auto;
    font-size: 12px;
    font-weight: 600;
    color: var(--gray-400);
    display: flex;
    align-items: center;
    gap: 6px;
  }
  .unused-body {
    display: none;
    padding: 0 16px 14px;
    border-top: 1px solid var(--gray-100);
  }
  .unused-sec.open .unused-body { display: block; padding-top: 12px; }
  .unused-hint {
    font-size: 11px;
    color: var(--gray-400);
    margin-bottom: 10px;
  }
  .unused-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
  }
  .unused-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--gray-50);
    border: 1.5px solid var(--gray-200);
    border-radius: 999px;
    padding: 6px 12px;
    font-size: 12px;
    font-weight: 600;
    color: var(--gray-600);
    cursor: pointer;
    transition: border-color 0.15s, background 0.15s;
  }
  .unused-chip:hover, .unused-chip.selected {
    border-color: var(--red);
    background: var(--red-soft);
    color: var(--red);
  }
  .unused-chip .av {
    width: 20px;
    height: 20px;
    border-radius: 50%;
    background: var(--gray-300);
    color: white;
    font-size: 9px;
    font-weight: 800;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .unused-chip.selected .av { background: var(--red); }

  /* Unassigned load chips (for drag/click assign) */
  .unassigned-loads {
    display: none;
    background: white;
    border: 1.5px dashed var(--gray-300);
    border-radius: var(--radius-lg);
    padding: 12px 16px;
    gap: 8px;
    flex-wrap: wrap;
    align-items: center;
  }
  .unassigned-loads.visible { display: flex; }
  .unassigned-loads .ulabel {
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 0.04em;
    color: var(--gray-400);
  }
  .load-chip-draggable {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--red-soft);
    border: 1.5px solid var(--red);
    color: var(--red);
    border-radius: 8px;
    padding: 7px 12px;
    font-size: 12px;
    font-weight: 700;
    cursor: grab;
    user-select: none;
  }
  .load-chip-draggable:active { cursor: grabbing; }
  .load-chip-draggable.selected {
    outline: 2px solid var(--red);
    outline-offset: 2px;
  }
  .load-chip-draggable.dragging { opacity: 0.5; }

  /* Assigned Drivers section */
  .section-head {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 4px;
  }
  .section-head h3 {
    font-size: 16px;
    font-weight: 800;
    color: var(--black);
  }
  .section-head .sub {
    font-size: 12px;
    color: var(--gray-400);
    font-weight: 500;
